# Ruff — keep CI lint light and practical for this dashboard
line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "I", "W"]
ignore = [
  "E501",  # line length handled separately / templates-style strings
]

[lint.per-file-ignores]
"tests/*" = ["E402"]
